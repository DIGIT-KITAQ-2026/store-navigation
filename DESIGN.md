# store-navigation デザインシステム(消費者画面)

このドキュメントは、消費者向け3画面(`/`, `/search`, `/navigate/[productId]`)の見た目を
**このファイルだけを見て再現できる**ことを目標に記述する。

- 手書き設計案(`art/消費者用画面UI設計案/`)をもとに、Google Stitch(MCP経由)でモバイル版・
  デスクトップ版のデザインをそれぞれ生成し、ユーザーレビュー・フィードバックを経て確定した内容
- 対象は消費者画面のみ。管理者画面(`/admin/*`)は対象外(将来別途デザインする)
- 実装は1つのNext.jsコンポーネントをTailwindのレスポンシブクラス(`md:`)で出し分ける想定。
  「モバイル版」「デスクトップ版」は同一ページの2つのブレークポイント表示であり、
  別ページではない
- ブレークポイント: Tailwindのデフォルト`md`(768px)を境に切り替える。768px未満をモバイルレイアウト、
  768px以上をデスクトップレイアウトとする

## 目次

1. [デザイントークン](#1-デザイントークン)
2. [共通コンポーネント](#2-共通コンポーネント)
3. [画像アセット](#3-画像アセット)
4. [画面仕様: `/`(トップページ)](#4-画面仕様-トップページ)
5. [画面仕様: `/search`(検索結果画面)](#5-画面仕様-検索結果画面)
6. [画面仕様: `/navigate/[productId]`(案内画面)](#6-画面仕様-案内画面)
7. [アイコン一覧](#7-アイコン一覧)
8. [既知の制約・意図的な逸脱](#8-既知の制約意図的な逸脱)

---

## 1. デザイントークン

### 1.1 カラーパレット

Stitchで生成した6画面(モバイル/PC × トップ・検索・案内)から実際に使われた値を集約し、
表記ゆれ(生成のたびに微妙に異なる近似値になっていた)を正規化した、正式なトークン値。

| トークン名 | HEX | 用途 |
|---|---|---|
| `primary` | `#12B76A` | ブランドカラー。プライマリボタン、アクティブ状態、リンクホバー、アクセントアイコン |
| `on-primary` | `#FFFFFF` | `primary`背景の上に乗るテキスト・アイコン |
| `primary-container` | `#D1FADF` | 薄い緑背景(位置情報バッジ、棚IDチップ、検索例チップの強調状態など) |
| `on-primary-container` | `#039855` | `primary-container`の上に乗るテキスト・アイコン(濃い緑) |
| `surface` | `#FFFFFF` | 画面・カードの基本背景色(白) |
| `on-surface` | `#101828` | 主要テキスト(見出し・本文の濃色) |
| `surface-variant` | `#F2F4F7` | ごく薄いグレー背景(入力欄の背景、地図プレースホルダーの背景など) |
| `on-surface-variant` | `#475467` | 副次テキスト(説明文、キャプション) |
| `outline` | `#94A3B8` | 強めのボーダー(入力欄フォーカス前の枠線など) |
| `outline-variant` | `#EAECF0` | 通常のボーダー(カード枠線、ヘッダー下線) |

**背景色に関する注意**: 検索結果画面は一時`surface-variant`相当の暗めのグレー背景になっていた
不具合があったが、修正済み。**全画面共通で、ページ背景は`surface`(白)に統一する**
(検索結果画面もトップページ・案内画面と同じ白背景にすること)。

**配色ブレに関する注意**: 検索結果画面の商品カードのカテゴリバッジ・位置情報バッジ・
「場所を見る」ボタンは、カードごとに緑/グレーが混在する不具合が過去にあったが、修正済み。
**すべてのカードで`primary`系の配色(バッジは`primary-container`背景+`on-primary-container`
または`primary`文字、ボタンは`primary`背景+白文字)に統一すること**。カテゴリによって色を
変える演出はしない。

### 1.2 タイポグラフィ

- **フォントファミリー**: `Public Sans`(Google Fonts) を基本とし、日本語文字はシステムの
  日本語フォントにフォールバックさせる。実装時のCSS指定:
  ```css
  font-family: "Public Sans", "Hiragino Kaku Gothic ProN", "Hiragino Sans",
    "Noto Sans JP", Meiryo, sans-serif;
  ```
  (`Public Sans`は英数字・ラテン文字にのみ効き、日本語部分は自動的にフォールバックフォントで
  描画される。これは意図的な選択であり、日本語用に別途Webフォントを読み込む必要はない)
- **見出し**: `font-headline` / `font-display` として`Public Sans`を使う(本文と同じファミリーで、
  太さ(`font-bold`/`font-black`)とサイズで階層を作る)
- **サイズ階層**(実際に使われた値):
  | 用途 | モバイル | デスクトップ |
  |---|---|---|
  | 店名(ヘッダー) | `text-xl font-bold`(約1.25rem) | `text-xl font-black`(約1.25rem) |
  | ページ主見出し(トップの店名見出し) | `text-2xl font-bold` | `text-4xl font-black` |
  | セクション見出し(「商品で探す」等) | `text-lg font-bold` | `text-3xl font-bold` |
  | 商品名(カード内) | `text-lg font-bold` | `text-lg font-bold` |
  | 本文・説明文 | `text-sm` | `text-lg`〜`text-xl` |
  | キャプション・バッジ文字 | `text-xs`〜`text-sm` | `text-xs`〜`text-sm` |

### 1.3 角丸(roundness)

Stitch側の設定は`ROUND_TWELVE`だが、実際に生成されたTailwind設定は以下の3段階
(プロジェクトの`ROUND_TWELVE`は「最大12px相当、ピルは別扱い」という意味で、実際の値は
生成結果からこの3段階に固定されている):

| トークン名 | 値 | 用途 |
|---|---|---|
| `rounded`(DEFAULT) | `0.5rem`(8px) | 小さめの要素(カテゴリバッジ、位置情報バッジの内側の角) |
| `rounded-lg` | `1rem`(16px) | ボタン、位置情報ブロック、フローティングカード |
| `rounded-xl` | `1.5rem`(24px) | 商品カード、案内画面の情報カード、3Dマップビューアーの外枠 |
| `rounded-full` | `9999px` | 検索例チップ、検索バー(ピル型)、円形ボタン(送信ボタン・戻るボタン・アイコンボタン) |

トップページのヒーロー写真の角丸のみ例外で `rounded-2xl`(モバイル)/ `rounded-[24px]`
(デスクトップ)を使う(商品カードと同格の`rounded-xl`相当)。

### 1.4 シャドウ

| 用途 | 値の目安 |
|---|---|
| カード・入力欄の通常影 | `shadow-sm` |
| ホバー時の強調影 | `shadow-md` |
| モバイル固定検索バー・案内画面フローティングカードの強い影 | `shadow-lg`〜`shadow-xl`、
  またはモバイル検索バーは`shadow-[0_8px_30px_rgb(0,0,0,0.12)]`(カスタム値) |

### 1.5 実装への落とし込み方(Tailwind CSS v4)

このプロジェクトは`src/app/globals.css`の`@theme inline`でCSS変数ベースのテーマを定義する
方式(Tailwind v4)。実装時は上記1.1〜1.3のトークンを次のように追加する:

```css
:root {
  --color-primary: #12B76A;
  --color-on-primary: #ffffff;
  --color-primary-container: #d1fadf;
  --color-on-primary-container: #039855;
  --color-surface: #ffffff;
  --color-on-surface: #101828;
  --color-surface-variant: #f2f4f7;
  --color-on-surface-variant: #475467;
  --color-outline: #94a3b8;
  --color-outline-variant: #eaecf0;
}

@theme inline {
  --color-primary: var(--color-primary);
  --color-on-primary: var(--color-on-primary);
  --color-primary-container: var(--color-primary-container);
  --color-on-primary-container: var(--color-on-primary-container);
  --color-surface: var(--color-surface);
  --color-on-surface: var(--color-on-surface);
  --color-surface-variant: var(--color-surface-variant);
  --color-on-surface-variant: var(--color-on-surface-variant);
  --color-outline: var(--color-outline);
  --color-outline-variant: var(--color-outline-variant);
}
```

これにより`bg-primary`, `text-on-surface-variant`, `border-outline-variant`のような
Tailwindユーティリティクラスがそのまま使えるようになる(既存の`bg-blue-600`等のTailwind
デフォルト色は、消費者画面のコンポーネントからは使わない)。

---

## 2. 共通コンポーネント

### 2.1 ヘッダー(StoreHeader) — 3画面すべて共通

- モバイル: `fixed`または`sticky` top-0、高さ`h-16`(64px)、`bg-surface`、`shadow-sm`
  (トップページのみ`fixed`+影、検索・案内画面は`sticky`+下線`border-b border-outline-variant`。
  実装上はどちらも「常に画面上部に張り付く」動作なので`sticky top-0 z-40`で統一してよい)
- デスクトップ: `sticky top-0 z-50`、`bg-surface`、`border-b border-outline-variant`、
  `shadow-sm`、左右パディング`px-8`、上下パディング`py-4`
- 中身: 左寄せで「ロゴ画像(`w-8 h-8`、`object-contain`) + 店名テキスト」のみ
  - 店名は店舗の実データ(Supabaseの`stores.name`)を表示するプレースホルダーで、
    デザイン確定時の仮の値は「グリーンマート 芝公園店」
  - 店名の文字色は必ず`text-on-surface`(黒系)。`text-primary`(緑)にしない
    (過去に緑になる不具合があったため明記)
- **右側には何も置かない**: 通知ベル・検索アイコン・ログインボタン・
  「ホーム/店舗情報/使い方/マイページ」等のナビゲーションリンクは一切表示しない
  (このアプリにはログイン・会員ページ機能が存在しないため)
- ヘッダーをタップ/クリックするとトップページ(`/`)に戻る

### 2.2 ボタン

| 種類 | スタイル | 用途例 |
|---|---|---|
| プライマリボタン(角丸) | `bg-primary text-on-primary rounded-lg py-3〜4 px-4〜6 font-bold`、
  ホバーで`hover:bg-primary/90`や`hover:shadow-lg` | 「場所を見る」(デスクトップグリッド版)、
  「3D案内を開始」 |
| プライマリボタン(ピル) | `bg-primary text-on-primary rounded-full px-4 py-2 font-medium` | 「場所を見る」
  (モバイルカード版) |
| セカンダリ/チップボタン | `bg-surface border border-outline-variant rounded-full px-4〜6 py-2`、
  ホバーで`hover:border-primary hover:text-primary` | 検索例チップ |
| アイコンボタン(円形) | `w-10 h-10 rounded-full flex items-center justify-center`、
  ホバーで`hover:bg-surface-variant` | 戻るボタン |
| 送信ボタン(円形・アクセント) | `w-10〜12 h-10〜12 rounded-full bg-primary text-on-primary`、
  中央に紙飛行機アイコン(`send`) | 検索バー右端(トップページ・検索結果画面共通) |

### 2.3 カード

- 基本形: `bg-surface rounded-xl border border-outline-variant shadow-sm p-4〜6`
- ホバー時: `hover:shadow-md`、稀に`hover:border-primary`
- 商品カード内の構成順序(共通): カテゴリ/位置バッジ → 商品名(太字) → 説明文 → 区切り →
  位置情報ブロック → アクションボタン

### 2.4 バッジ/チップ

- 位置情報バッジ: `bg-primary-container text-on-primary-container` または
  `bg-primary-container/30 text-primary`、`rounded-md`または`rounded-lg`、
  アイコン`location_on`付き
- 棚IDチップ: `bg-primary-container text-on-primary-container rounded-full px-3 py-1 text-sm`、
  アイコン`shelves`付き、テキストは「棚ID: Shelf_12」の形式
- 検索例チップ: 2.2の「セカンダリ/チップボタン」と同じ

### 2.5 検索バー

2種類ある。用途を混同しないこと。

1. **トップページの検索バー**(虫眼鏡+テキスト入力+円形の送信ボタン):
   - モバイル: 画面下部に`fixed`、ピル型(`rounded-full`)、`bg-surface`、強めの影
   - デスクトップ: ページ内コンテンツとして中央配置、同じくピル型
   - プレースホルダー: 「商品名や欲しいものを入力」
   - 送信ボタンはAI検索(ネイティブ`<form method="GET" action="/search">`)を実行する
2. **検索結果画面の検索バー**(再検索用):
   - モバイル: 画面下部に`fixed`、ピル型、右端に送信アイコンボタン
   - デスクトップ: ページ上部にインラインで配置、角丸`rounded-xl`の入力欄(ピルではない)
   - 送信ボタンの見た目はトップページと共通(紙飛行機アイコン)で統一する

### 2.6 3Dマップビューアー

- ラベルチップ「3D店内マップ」(アイコン`3d_rotation`)を左上または右上に重ねて表示
- 中身は実装時にUnity WebGLのiframeに置き換わるプレースホルダー領域
- **アスペクト比の制約(重要)**: `public/unity/index.html`のUnityカメラは16:9横長を前提と
  しており、極端な縦長・正方形のコンテナにすると3Dシーンの一部しか描画されない実機バグが
  過去に発生している。モバイルでは`aspect-video`(16:9)を厳守し、デスクトップの全画面表示
  レイアウト(6.2節参照)でも、Unity本体の描画がこの制約に収まっているか実機で確認すること

---

## 3. 画像アセット

Stitchでの生成物のうち、実装にそのまま使える画像を`public/images/design-reference/`に
保存済み。すべて仮のプレースホルダー(実店舗写真ではない)。

| ファイル | 内容 | 使用箇所 |
|---|---|---|
| `logo.png` | 緑のグラデーション、ショッピングカート+位置ピンを組み合わせた円形ロゴ | 全画面共通ヘッダー |
| `store-hero-photo.jpg` | 実写風の店舗外観写真(モバイル・デスクトップ共通、同一画像) | トップページのヒーロー画像 |
| `store-illustration.jpg` | フラットベクター風の店舗外観イラスト | 案内画面(デスクトップ)の3Dマップ
  プレースホルダー画像 |
| `navigate-map-placeholder.jpg` | アイソメトリック(見下ろし)の店内通路イラスト、
  目的地ピン付き | 案内画面(モバイル)の3Dマッププレースホルダー画像 |

実装時は`<Image src="/images/design-reference/logo.png" ...>`のように参照する。
実店舗写真・実店内マップ画像の用意は本デザインのスコープ外。

---

## 4. 画面仕様: トップページ(`/`)

### 4.1 構成(共通の並び順)

1. ヘッダー(2.1節)
2. ヒーローセクション: 店舗写真 → 店名(見出し) → 店舗説明文
3. 検索セクション: 見出し「商品で探す」+説明文 → 検索例チップ
4. 検索バー(2.5節の1.)

**この4ブロック以外の要素(「よく行くコーナー」等のショートカットカード、下部タブバー等)は
追加しないこと**。過去にこれらを含むバージョンを作ったが、ユーザー確認の結果すべて削除する
方針が確定している。

### 4.2 コピー(確定文言・プレースホルダー)

- 店名(見出し・ヘッダー共通): 「グリーンマート 芝公園店」(実装時はSupabaseの店舗名に置換)
- 店舗説明文: 「営業時間: 9:00-22:00 | 地元直送の新鮮な野菜とこだわりのお惣菜が自慢の店舗です。」
  (実装時はSupabaseの店舗説明フィールドに置換)
- セクション見出し: 「商品で探す」
- セクション説明文: 「商品名や作りたいもの・目的から売り場を検索できます。」
- 検索例チップ(5個、順番通り): 牛乳 / カレーが食べたい / 掃除用品 / お惣菜 / 特売品
- 検索バーのプレースホルダー: 「商品名や欲しいものを入力」

### 4.3 モバイルレイアウト

```
┌─────────────────────────────┐
│ [ロゴ] グリーンマート 芝公園店   │  ← header, h-16, fixed top, bg-surface, shadow-sm
├─────────────────────────────┤
│  ┌───────────────────────┐  │
│  │   店舗写真 (rounded-2xl)  │  │  max-h-64, object-cover
│  └───────────────────────┘  │
│      グリーンマート 芝公園店     │  ← text-2xl font-bold, 中央揃え
│   営業時間: 9:00-22:00 | ...  │  ← text-sm text-on-surface-variant, 中央揃え
│                               │
│  🔍 商品で探す                 │  ← text-lg font-bold + icon manage_search
│  商品名や作りたいもの...        │  ← text-xs
│  [牛乳][カレーが食べたい][...] │  ← 横スクロールチップ(overflow-x-auto)
├─────────────────────────────┤
│ 🔍 商品名や欲しいものを入力  [➤]│  ← fixed bottom-6、ピル型、影付き
└─────────────────────────────┘
```

- コンテナ: `pt-16 px-4 max-w-2xl mx-auto`(ヘッダー分の上余白を確保)
- 本文下に`pb-32`相当の余白を確保し、固定検索バーとコンテンツが重ならないようにする
- 検索例チップは横スクロール可能(`overflow-x-auto hide-scrollbar`)。5つ全部が一度に
  収まらなくてよい

### 4.4 デスクトップレイアウト

```
┌───────────────────────────────────────────────────┐
│ [ロゴ] グリーンマート 芝公園店                          │  ← header, px-8 py-4
├───────────────────────────────────────────────────┤
│              ┌─────────────────────────┐             │
│              │   店舗写真 (16:9〜21:9)     │             │  max-w-5xl, rounded-[24px]
│              └─────────────────────────┘             │
│                グリーンマート 芝公園店                    │  ← text-4xl font-black, 中央揃え
│           営業時間: 9:00-22:00 | ...                  │  ← text-lg〜xl, 中央揃え
│                                                       │
│        [🔍 商品名や欲しいものを入力      [➤]]           │  ← max-w-4xl, ピル型検索バー(インライン)
│                                                       │
│                   商品で探す                           │  ← text-3xl font-bold, 中央揃え
│         商品名や作りたいもの・目的から...                 │
│      [牛乳] [カレーが食べたい] [掃除用品] [お惣菜] [特売品]  │  ← flex-wrap、中央揃え
└───────────────────────────────────────────────────┘
```

- 全体を`flex flex-col items-center`で中央揃えにする、セクションごとに`max-w-*`で幅を制限
- 検索バーはトップページ「内」の要素として通常フローに配置(モバイルのような`fixed`ではない)
- フッターは配置しない

---

## 5. 画面仕様: 検索結果画面(`/search`)

### 5.1 構成(共通の並び順)

1. ヘッダー(2.1節、トップページと共通)
2. (デスクトップのみ)再検索用の検索バーをヘッダー直下に配置
3. 見出し行: 戻るボタン + 「"{検索クエリ}" の検索結果」
4. 商品カードのリスト/グリッド
5. (モバイルのみ)画面下部固定の再検索バー

### 5.2 コピー

- 見出し: `"{{query}}" の検索結果`(`{{query}}`部分のみ`text-primary`で強調、残りは
  `text-on-surface-variant`)
- カードのボタン文言: 「場所を見る」
- サンプルデータ(検索クエリ「カレーが食べたい」の場合):
  | 商品名 | 説明 | 売り場 | カテゴリ(デスクトップのみ表示) |
  |---|---|---|---|
  | カレールー 中辛 | スパイシーな定番ルー | 3番通路 香辛料・調味料コーナー | 食品(アイコン`local_mall`) |
  | 北海道産たまねぎ | カレーに欠かせない甘み | 1番通路 野菜コーナー | 青果(アイコン`eco`) |
  | 牛肩ロース カレー用 | 煮込みに最適な赤身肉 | 精肉コーナー 奥 | 精肉(アイコン`restaurant`) |

### 5.3 商品カードの構造

**商品画像(サムネイル)は表示しない**(実装済みデータに商品画像を含める予定がないため)。

- モバイル: 縦1列リスト。カード全体がリンク(`<a>`)になっており、カードのどこをタップしても
  該当商品の案内画面に遷移する。カード内に「場所を見る」ボタンも別途表示するが、これは
  視覚的な誘導であり、ボタン自体もカード全体と同じ遷移先でよい
  - カード構成: 商品名(`text-lg font-bold`) → 説明文(`text-sm`、2行まで) →
    位置バッジ(アイコン`location_on` + 「○番通路 ○○コーナー」、`bg-primary-container
    text-primary rounded-md`) → 「場所を見る」ボタン(ピル型、右寄せまたは下寄せ)
  - カテゴリバッジ(食品/青果/精肉)は**モバイルでは表示しない**(情報量を抑えるため)
- デスクトップ: 3列グリッド(`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`)。
  カードはリンクではなくボタン操作(「場所を見る」ボタンで遷移)。
  - カード構成: カテゴリバッジ(アイコン+「食品」等、`bg-primary-container/30 text-primary
    rounded-md`) → 商品名 → 説明文 → 位置情報ブロック(`bg-primary-container/30 rounded-lg
    p-3`、通路番号を太字1行目・コーナー名を2行目) → 「場所を見る」ボタン(角丸`rounded-lg`、
    幅いっぱい、`bg-primary text-on-primary`)

### 5.4 空状態・0件の場合

このデザインパスでは詳細ビジュアルは作成していないが、既存実装(`EmptyState`コンポーネント)
の文言・体裁をそのまま流用してよい。トークン(2.1〜2.4節)にだけ合わせて配色を統一すること。

---

## 6. 画面仕様: 案内画面(`/navigate/[productId]`)

### 6.1 構成(共通の並び順)

1. ヘッダー(2.1節、トップページと共通)
2. 戻る導線: 「← 検索結果に戻る」
3. 3Dマップビューアー(2.6節)
4. 商品情報(商品名・分類・棚IDチップ・場所の説明)
5. プライマリボタン「3D案内を開始」

**モバイルとデスクトップでは4と5の配置方法が異なる**(6.3節参照)。

### 6.2 コピー

- 戻る導線の文言: 固定で「検索結果に戻る」(検索クエリを含めない。案内画面はどのクエリから
  来たかを覚えていなくてもよい導線として、常にこの文言でよい)
- 3Dマップのラベル: 「3D店内マップ」
- サンプル商品情報:
  - カテゴリ(パンくず): 「調味料 > カレールー」(モバイルのみ表示)/「調味料・カレールー」
    (デスクトップの短縮形、モバイルでは`>`区切り)
  - 商品名: 「カレールー 中辛」
  - 棚IDチップ: 「棚ID: Shelf_12」
  - 場所の説明: 「入口から直進し、3番通路の右側です。」
  - ボタン文言: 「3D案内を開始」(アイコン`navigation`)

### 6.3 モバイルレイアウト(縦積み)

```
┌─────────────────────────────┐
│ [ロゴ] グリーンマート 芝公園店   │  ← header
├─────────────────────────────┤
│ ←  検索結果に戻る               │  ← 戻る導線(ヘッダー下の別行)
│  ┌───────────────────────┐  │
│  │ 3D店内マップ               │  │  ← aspect-video, rounded-xl, border-2
│  │  (アイソメトリック地図)       │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 調味料 > カレールー         │  │  ← 情報カード(白背景、rounded-xl、shadow-sm)
│  │ カレールー 中辛(大きめ見出し)  │  │
│  │ [棚ID: Shelf_12]           │  │
│  │ 🚶 入口から直進し、3番通路の... │  │
│  └───────────────────────┘  │
│  [🧭 3D案内を開始]              │  ← 幅いっぱいのプライマリボタン
└─────────────────────────────┘
```

- 3Dマップと情報カード・ボタンは**通常の縦フロー**で並ぶ(オーバーレイにしない)。
  Unity表示のアスペクト比を保つため、`aspect-video w-full`のまま画面全体を覆う
  フルスクリーン化はしない(過去にこの方式で3D描画が崩れる実機バグが発生したため)

### 6.4 デスクトップレイアウト(マップ全面+右下フローティングカード)

```
┌───────────────────────────────────────────────────┐
│ [ロゴ] グリーンマート 芝公園店                          │  ← header
├───────────────────────────────────────────────────┤
│ [← 検索結果に戻る]                      [3D店内マップ]     │  ← 両方ともマップの上に浮くチップ/ピル
│                                                       │     (左上・右上、半透明白背景+blur)
│                                                       │
│           3Dマップビューアー(画面幅いっぱい・              │  ← 残り高さ全体、object-cover
│              ヘッダー分を除く縦幅いっぱい)                  │
│                                                       │
│                                    ┌───────────────┐  │
│                                    │ カレールー 中辛    │  │  ← フローティングカード
│                                    │ [棚ID:Shelf_12]  │  │     w-80、右下 bottom-8 right-8
│                                    │ 入口から直進し...   │  │     bg-surface, rounded-xl,
│                                    │[🧭 3D案内を開始]   │  │     shadow-xl, border
│                                    └───────────────┘  │
└───────────────────────────────────────────────────┘
```

- **重要**: この案内画面(デスクトップ)は、情報量が少ないという判断から**サイドパネル方式
  ではなく右下フローティングカード方式を採用する**(検討の結果、サイドパネル案は不採用)。
  3Dマップは左右分割せず、ヘッダーを除く画面全体(`w-full h-[calc(100vh-65px)]`程度)を使う
- 戻る導線: マップ左上に絶対配置。半透明白背景+`backdrop-blur-sm`、ピル型、
  `border border-outline-variant`、影付き。アイコン`arrow_back`+テキスト「検索結果に戻る」
- 「3D店内マップ」ラベル: マップ右上に絶対配置。同様の半透明白背景+blur、角丸`rounded-lg`
- フローティング情報カード: マップの右下に絶対配置(`absolute bottom-8 right-8`)、
  幅`w-80`(320px)程度のコンパクトな1枚のカード。商品名・棚IDチップ・場所説明・
  「3D案内を開始」ボタンをすべてこのカード内にまとめる(サイドパネルのように分割しない)
- カード出現時に軽いフェードインアップアニメーション(`opacity: 0→1`, `translateY(20px→0)`,
  0.5s ease-out)を付けてもよい(必須ではない)

---

## 7. アイコン一覧

Stitch側は Google の **Material Symbols Outlined**(アイコンフォント)を使用している。
実装時も同じ見た目にする場合は、`next/head`相当の方法で以下を読み込む:

```html
<link rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap">
```

使用箇所と対応アイコン名一覧:

| アイコン名 | 用途 |
|---|---|
| `search` | 検索バーの虫眼鏡 |
| `send` | 検索バーの送信ボタン(紙飛行機。トップページ・検索結果画面共通) |
| `arrow_back` | 戻るボタン全般 |
| `manage_search` | トップページ「商品で探す」見出し(モバイル) |
| `location_on` | 位置情報バッジ |
| `map` | 「場所を見る」ボタン |
| `local_mall` | カテゴリバッジ「食品」 |
| `eco` | カテゴリバッジ「青果」 |
| `restaurant` | カテゴリバッジ「精肉」 |
| `shelves` | 棚IDチップ |
| `directions_walk` | 案内画面の道順説明(モバイル) |
| `3d_rotation` | 「3D店内マップ」ラベル |
| `navigation` | 「3D案内を開始」ボタン |

**代替案**: アイコンフォントの追加読み込みをせず、プロジェクトで別のアイコンライブラリ
(例: `lucide-react`)を使う場合は、上記アイコン名に見た目が近いものへ読み替えてよい
(例: `location_on`→`MapPin`、`arrow_back`→`ArrowLeft`、`shelves`→`LayoutGrid`等)。
どちらの方式を採るかは実装フェーズの最初に決めること。

---

## 8. 既知の制約・意図的な逸脱

- **Unity 3D表示のアスペクト比**: `public/unity/index.html`のカメラは16:9横長前提。
  モバイルは`aspect-video`固定、デスクトップも実機で描画崩れがないか必ず確認すること
  (3.5節・6.4節参照)
- **画像アセットは仮のプレースホルダー**: `store-hero-photo.jpg`等は実店舗写真ではない。
  実店舗写真を用意でき次第、`public/images/design-reference/`のファイルを差し替える
  (パスは変えなくてよい)
- **検索例チップの文言はダミー**: 「牛乳」「カレーが食べたい」等は例示用。実際に店舗で
  検索されやすいキーワードに実装時差し替えてよい
- **Stitch生成時の英語混入・軽微な表記ゆれ**: Stitchでの生成過程で一部英語表記
  (「Back to search results」等)が混ざる不具合があったが、本ドキュメントの4〜6節は
  すべて修正後の日本語表記で記載している。実装はこのドキュメントの記載を正としてよい
- **管理者画面は対象外**: `/admin/*`は本デザインの対象に含まれない(プレースホルダーの
  ままでよい)
