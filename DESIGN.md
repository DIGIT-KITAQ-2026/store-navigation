---
product: store-navigation
description: 店内商品検索・ナビゲーションアプリのUIデザインシステム
tokens:
  color:
    primary: "#0B6E6E"        # メイン操作(検索実行, ナビ開始などのCTA)
    primary-hover: "#095858"
    accent-ai-start: "#6D5EF0"  # AI検索エリアのグラデーション開始色
    accent-ai-end: "#22B8CF"    # AI検索エリアのグラデーション終了色
    surface: "#FFFFFF"        # カード, ボトムシート, 入力欄の背景
    surface-alt: "#F1F3F4"    # 画面背景, 非活性の検索バー背景
    surface-pill: "#DFF3F1"   # pill型アクションボタンの背景(出発/経路案内などに相当)
    text-primary: "#1F2124"
    text-secondary: "#5F6368"
    text-on-primary: "#FFFFFF"
    border: "#E0E2E5"
    success: "#1E8E3E"
    danger: "#D93025"         # 削除など破壊的操作
  typography:
    font-family: "Noto Sans JP, system-ui, sans-serif"
    h1: { size: 24px, weight: 700, line-height: 1.3 }
    h2: { size: 18px, weight: 700, line-height: 1.4 }
    body: { size: 15px, weight: 400, line-height: 1.6 }
    caption: { size: 12px, weight: 400, line-height: 1.4, color: text-secondary }
    button: { size: 15px, weight: 600, line-height: 1 }
  spacing:
    unit: 4px
    scale: [4, 8, 12, 16, 24, 32, 48]
  radius:
    pill: 999px      # 検索バー, アクションボタン, チャット入力欄
    card: 16px        # ボトムシート, カード
    control: 12px     # 通常のボタン, 入力フィールド
  shadow:
    sheet: "0 -2px 12px rgba(0,0,0,0.12)"
    floating-button: "0 2px 8px rgba(0,0,0,0.2)"
  motion:
    sheet-transition: "280ms cubic-bezier(0.2, 0, 0, 1)"
    press-scale: "0.97 / 100ms"
---

# store-navigation デザインシステム

## 1. プロダクトのトーン

店員に話しかけずに、スマホだけで欲しい商品を見つけて棚まで辿り着ける体験を提供する。ターゲットはコミュニケーションを取りたくない/急いでいる消費者と、業務を中断させたくない店員。UIは「調べる→分かる→着く」の3ステップが迷いなく進むことを最優先し、装飾よりも操作導線の分かりやすさを優先する。

参考にした外部UI(`art/外部UI設計/`)から次の3パターンを踏襲する。

- **Googleマップの検索画面**(`IMG_2228`): 角丸pill型の検索バー + 履歴/候補リストで、次のアクションを迷わず選べる
- **Googleマップの経路結果画面**(`IMG_2229`): 地図の上にかぶさるボトムシート、pill型のアクションボタン群、右下のフローティングアクションボタン(現在地ボタン相当)
- **Geminiの会話画面**(`IMG_2230`): グラデーション背景 + 一言の語りかけ + 丸いチャット入力欄という、AIとの対話であることが一目で分かる見た目

## 2. デザイン原則

1. **検索窓を常に主役にする** — トップ画面・AI検索画面ともに、pill型の検索/入力欄を画面下部または上部に大きく配置する
2. **地図・ナビゲーションは沈める、操作は浮かせる** — Unity 3Dナビゲーション画面はフルスクリーン表示し、検索結果や操作ボタンはボトムシート/フローティングボタンとして地図の上に重ねる
3. **AI領域はグラデーションで区別する** — Claudeが応答する検索エリアには`accent-ai-start`→`accent-ai-end`のグラデーションを使い、通常のナビゲーション画面(teal基調)と視覚的に区別する
4. **すべての主要操作はpillボタンにする** — 出発/経路案内/削除選択などの主要アクションは`radius.pill`+`color.surface-pill`のボタンで統一する
5. **タップ領域は44px以上を確保する** — バーコードスキャンや棚選択など片手操作が多いため、タップ対象は最低44×44pxを確保する

## 3. カラー

| ロール | 値 | 用途 |
|---|---|---|
| primary | `#0B6E6E` | 検索実行ボタン、ナビ開始ボタンなどの主要CTA |
| accent-ai-start / accent-ai-end | `#6D5EF0` → `#22B8CF` | AI検索画面の背景グラデーション、AIが応答中であることを示すアクセント |
| surface | `#FFFFFF` | ボトムシート、カード、入力欄の背景 |
| surface-alt | `#F1F3F4` | 画面全体の背景、非活性状態の検索バー |
| surface-pill | `#DFF3F1` | pill型アクションボタンの背景(出発・経路案内・保存などに相当する操作) |
| text-primary / text-secondary | `#1F2124` / `#5F6368` | 本文/補足テキスト |
| danger | `#D93025` | 商品削除など破壊的な操作の強調 |

AI検索画面以外(管理者画面・ナビゲーション画面)ではグラデーションを使わず、primaryのteal一色で統一する。グラデーションはAIとの対話中である画面にのみ使用する。

## 4. タイポグラフィ

- フォント: `Noto Sans JP` を基本とし、和文の可読性を優先する
- 見出し(h1: 24px/700, h2: 18px/700)は画面タイトルのみに使用し、多用しない
- 本文は15px/400、行間1.6で、検索結果や商品説明など読み物になる箇所に使う
- caption(12px)は棚番号・距離・タイムスタンプなど補足情報に限定する

## 5. スペーシング

4pxを基準単位とし、`[4, 8, 12, 16, 24, 32, 48]`のスケールから選ぶ。ボトムシート内の要素間は16px、セクション間は24〜32pxを基本とする。

## 6. コンポーネント

- **検索バー / チャット入力欄**: `radius.pill`、左にアイコン(虫眼鏡またはAIスパークル)、右にマイクまたは送信アイコン。背景は非活性時`surface-alt`、フォーカス時`surface`+`border`
- **ボトムシート**: 上端`radius.card`の角丸、上部にドラッグハンドル、影は`shadow.sheet`。地図/Unityナビゲーション画面の上に重ねて表示する
- **pillアクションボタン**: アイコン+ラベルの横並び、背景`surface-pill`、文字色`primary`。出発・経路案内・共有・保存に相当する操作、および管理者画面の商品選択などに使う
- **フローティングアクションボタン**: 円形、`shadow.floating-button`、現在地に戻る・スキャン起動など単一の主要操作に使う。画面右下に固定配置
- **リストアイテム(商品一覧・検索結果)**: アイコン/サムネイル + タイトル + 補足テキストの1行〜2行構成。タップで詳細またはナビゲーション起動
- **破壊的操作ボタン(商品削除)**: `color.danger`を用い、誤操作防止のため確認ステップ(選択→確認)を挟む

## 7. レイアウト

- モバイル縦持ちのシングルカラムを基本とする
- 地図/Unityナビゲーションは背景全面に敷き、UIはその上にオーバーレイするレイヤー構成(Googleマップ型)
- 管理者画面(ログイン・登録・削除・一覧)はシンプルなフォーム/リスト中心のレイアウトとし、AI要素のグラデーションは使用しない

## 8. モーション

- ボトムシートの開閉は`280ms cubic-bezier(0.2, 0, 0, 1)`でスライド
- ボタン押下時は`0.97`にスケールダウン(100ms)し、押した手応えを出す
- AI応答待ちはスパークルアイコンの緩やかな明滅などで「考え中」を示し、スピナーは多用しない

## 9. アクセシビリティ

- 文字色と背景のコントラスト比はWCAG AAを満たす(本文は4.5:1以上)
- タップ対象は44×44px以上を確保する
- カメラでのバーコード/QRスキャン画面には、読み取り位置を示す枠と代替のテキスト入力手段を用意する

## 10. 禁止事項

- 通常画面(非AI)でのグラデーション多用
- 主要CTA以外への`danger`カラーの使用
- 情報量の多い管理者画面での過度なアニメーション
- pillボタン以外の角丸なしボタンを主要操作に使うこと(補助的なテキストリンクを除く)
