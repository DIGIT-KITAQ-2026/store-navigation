# 開発ルール(CONTRIBUTING)

このチームはGitを使った共同開発がほぼ初めてのメンバーで構成されています。難しいルールにはせず、
「これだけ守れば大きな事故(お互いの作業を消してしまう、コンフリクトで詰まる)が起きにくい」
という最低限のルールをまとめます。分からなくなったらこのファイルに戻ってきてください。

## 基本の流れ(1機能=1ブランチ+PR)

1. 作業を始める前に、必ず最新の `main` を取り込む

   ```
   git checkout main
   git pull
   ```

2. 自分がこれから作る機能ごとに、新しいブランチを作る(mainに直接コミットしない)

   ```
   git checkout -b feature/<名前>-<やること>
   # 例: feature/tanaka-search-ui
   ```

3. こまめに、小さい単位でコミットする(1つの機能を全部作ってから1回でコミット、はNG)
4. 早めに `git push -u origin <ブランチ名>` して、GitHub上でPull Request(PR)を作る
5. PRを `main` に取り込む(マージする)。可能なら他のメンバーにも軽く見てもらう

**ポイント**: 「ある程度まとまってからpushしよう」と思っていると、その間に他の人の変更と
食い違ってコンフリクトが起きやすくなります。**早く小さくpushする**方が結果的に楽です。

## ブランチ名・コミットメッセージ

- ブランチ名: `feature/<自分の名前かイニシャル>-<内容が分かる短い英語か日本語>`
- コミットメッセージ: 「何を」「なぜ」が分かる一言でOK。例:
  - `feat: 商品検索フォームを追加`
  - `fix: ログイン画面のバリデーション修正`
  - `docs: 仕様書を更新`

## やってはいけないこと

- `main` ブランチに直接コミットする
- 他の人が使っているブランチに `git push --force` する
- コンフリクトの解決に困ったときに、よく分からないまま `git checkout .` や `git reset --hard` をして
  変更を消してしまう(消す前に一旦Claudeやチームに相談する)

## すでにバラバラに作業してしまっている場合

もし `main` を共有しないまま個人のPC上で作業を進めてしまった場合は、`main`に直接push
しようとせず、`/merge-folder` スキルを使ってください。

```
/merge-folder <自分の作業フォルダの絶対パス>
```

`main` から新しいブランチを作った上で、あなたの作業フォルダとの差分を1件ずつ確認しながら
安全に取り込みます(勝手に上書き・削除はしません)。取り込み後は通常どおりpush→PRに進めます。

## コンフリクトが起きたら

コンフリクトは「悪いこと」ではなく、同じファイルを2人が触ったときに自然に起きるものです。
落ち着いて次の手順で対応してください。

1. コンフリクトが起きたファイルを開くと、次のような印が入っています

   ```
   <<<<<<< HEAD
   自分の変更
   =======
   相手の変更
   >>>>>>> feature/xxxx
   ```

2. 両方の内容を見て、最終的にどう書くべきかを決め、`<<<<<<<`・`=======`・`>>>>>>>` の印を
   すべて削除して正しい内容だけを残す
3. 直したファイルを保存し、動作確認(画面が表示される、エラーが出ない、など)する
4. `git add <ファイル>` してコミットし、コンフリクト解消を完了させる
5. 判断に迷う場合や、自分の変更と相手の変更のどちらを優先すべきか分からない場合は、
   無理に自己判断せずチームやClaudeに相談してから進める

## 次に何をやるか迷ったら

`TASK.md` に共有のタスク一覧があります。`/manage-tasks` スキルで一覧表示・追加・完了報告が
できます。担当は決めていないので、空いているタスクから自由に着手してください。
